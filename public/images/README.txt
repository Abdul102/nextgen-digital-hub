Drop your CEO photo here as `ceo.jpg` (or .png / .webp).

Recommended:
- Aspect ratio: 4:5 (portrait), e.g. 800 x 1000 px
- Format: JPG or WebP for fastest loading
- File size: under 500 KB

The website will automatically pick it up. If no file is here, a "AR"
gradient placeholder shows instead — no broken images.
